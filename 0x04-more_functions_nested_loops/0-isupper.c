#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: character
 *
 * Return: 1 if character is uppercase, esle 0
 */
int _isupper(int c)
{
	int c;

	if (c >= 'A' && c <= 'Z')
		return (1);
	esle
		return (0);
}
